# Same MongoDB cluster as apps/user
MONGODB_URI=mongodb+srv://nexabus:tanmay123@cluster0.nujuld3.mongodb.net/

# Same JWT secret as apps/user (tokens are role-scoped via the 'role' claim)
JWT_SECRET=competitor-of-bitlaa-coming-soon
