{
  "name": "nexabus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:user": "pnpm --filter user dev",
    "dev:operator": "pnpm --filter operator dev",
    "build:user": "pnpm --filter user build",
    "build:operator": "pnpm --filter operator build"
  }
}
